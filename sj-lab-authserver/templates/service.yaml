apiVersion: v1
kind: Service
metadata:
  name: {{ include "sj-lab-authserver.fullname" . }}
spec:
  selector:
    app: {{ include "sj-lab-authserver.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
