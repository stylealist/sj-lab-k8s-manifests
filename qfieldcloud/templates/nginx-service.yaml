apiVersion: v1
kind: Service
metadata:
  name: qfieldcloud-nginx
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: qfieldcloud-nginx
  ports:
    - port: 80
      targetPort: 80