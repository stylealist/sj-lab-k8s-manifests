apiVersion: v1
kind: Service
metadata:
  name: {{ include "sj-lab-mapservice-chart.fullname" . }}
spec:
  selector:
    app: {{ include "sj-lab-mapservice-chart.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
